version: '3.8'

services:
  needscareai:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      # Database configuration - REQUIRED
      - DATABASE_URL=${DATABASE_URL}
      - AWS_DATABASE_URL=${AWS_DATABASE_URL}
      # Session secret - REQUIRED
      - SESSION_SECRET=${SESSION_SECRET}
      # Email configuration - REQUIRED
      - GMAIL_EMAIL=${GMAIL_EMAIL}
      - GMAIL_APP_PASSWORD=${GMAIL_APP_PASSWORD}
      # OpenAI API - OPTIONAL
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      # Optional configurations
      - DISABLE_COMPOSITE_FK=${DISABLE_COMPOSITE_FK:-false}
    env_file:
      - .env.docker
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:5000/api/health', (res) => process.exit(res.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Optional: PostgreSQL database service
  # Uncomment if you want to run the database in Docker too
  # postgres:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_DB: needscareai
  #     POSTGRES_USER: needscareai_user
  #     POSTGRES_PASSWORD: ${DB_PASSWORD}
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"
  #   restart: unless-stopped

# volumes:
#   postgres_data: